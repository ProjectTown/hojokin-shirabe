# 需要検証プラン

最終更新: 2026-08-01
公開URL: https://hojokin-shirabe.pages.dev

収益化は行わない。需要があるかどうかだけを、実測値で判断する。
測定にトラッキングスクリプト・Cookie・個人情報を使わない。

---

## 1. 測定の考え方

行動が起きたら必ず専用のURLが1回開かれるようにサイトを設計してある。
その結果、**ページごとの表示回数という基本の数字だけ**で必要な指標が復元できる。

| 指標 | 取得方法 |
|---|---|
| 訪問者数 | 全ページの表示回数・参照元 |
| 検索を使った人数 | `/search/result/` の表示回数 |
| 補助金詳細を開いた人数 | `/s/*/` の表示回数 |
| 公式ページへ移動した人数 | `/go/*/` の表示回数（公式リンクは全てここを経由） |
| 検索結果ゼロ率 | `/search/none/` ÷ `/search/result/` |
| 地域別・業種別の利用状況 | `/area/*/` `/industry/*/` `/find/*/*/` の表示回数 |
| 再訪者数 | Cloudflare の集計（**参考値**） |

検索キーワードは記録しない。検索語はURLのハッシュ（`#q=`）に置いており、
ハッシュはHTTPリクエストに含まれないため、サーバーにも集計にも残らない。
「何回失敗したか」は分かるが「何を入力したか」は分からない。

---

## 2. 正式な30日計測の開始条件

**単に30日待つのではなく、下の3つを全て満たした日を Day 0 とする。**
満たさないうちは、計測期間に数えない。

| # | 開始条件 | 確認方法 | 状態 |
|---|---|---|---|
| A | 検索エンジンに掲載されている | `site:hojokin-shirabe.pages.dev` で **10ページ以上**がヒットする。またはSearch Consoleの「ページ」で登録済みが10件以上 | 未達 |
| B | 実利用者が訪れている | 内部テストを除いた訪問が **累計30以上**（流入元は問わない） | 未達 |
| C | 内部テストを除外できている | 下記のベースライン方式で除外可能な状態 | **達成** |

### C の除外方法

Cookieを使わない解析ではIP除外ができないため、**期間で切り分ける**。

1. 開発・検証中に発生したアクセスは、Day 0 より前として扱う
2. Day 0 の日時を `RESULTS.md` に記録し、その時点の累計表示回数を**ベースライン**として控える
3. 以降の集計は「現在の累計 − ベースライン」で計算する
4. Day 0 以降、私は自動ツールでサイトへアクセスしない。
   状態確認は解析画面の閲覧のみで行い、サイト本体にはアクセスしない

---

## 3. 30日後の評価基準

訪問者数の絶対値だけでは判断しない。**比率で「実際に使われたか」を見る。**

| 指標 | 計算式 | 継続の最低ライン | 理由 |
|---|---|---|---|
| 詳細ページ到達率 | `/s/` 表示 ÷ 訪問 | **25%以上** | 一覧で帰るなら中身に価値がない |
| **公式サイト遷移率** | `/go/` 表示 ÷ 訪問 | **8%以上** | **最重要。ここが動けば「実際に使われた」と言える** |
| 検索利用率 | `/search/result/` ÷ 訪問 | **15%以上** | 絞り込む動機があったか |
| 検索結果ゼロ率 | `/search/none/` ÷ `/search/result/` | **40%未満** | 高いなら掲載範囲が狭すぎる |
| 地域別・業種別の利用 | 上位ページの偏り | **2県以上・2業種以上**が使われている | 一部地域だけなら需要が局所的 |
| 訪問者数 | 実測 | **30日で100以上** | 母数が足りないと比率が信用できない |
| 再訪者数 | 実測 | 基準に使わない（**参考値**） | Cookieなし解析では精度に限界がある |

### 判定

- **公式サイト遷移率を含め、上の必須5項目を満たす** → 需要あり。次の段階へ
- **一部のみ満たす** → 原因を特定し、**改善を1回だけ**行い、さらに30日測る
- **改善後も満たさない** → **事業を終了**。コードと測定結果を保存し、次の事業案へ移る

### 改善が1回だけ許される想定パターン

| 症状 | 想定原因 | 打ち手（1つだけ選ぶ） |
|---|---|---|
| 訪問がほぼゼロ | 検索で見つからない | 掲載範囲をさらに広げる（募集終了分の履歴化など） |
| 検索結果ゼロ率が高い | 扱う制度の幅が足りない | 掲載範囲を広げる |
| 一覧で離脱 | 情報が薄い・見づらい | 詳細ページの構成を作り直す |
| 公式遷移が低い | 自分に関係ないと判断されている | 対象条件の表示を分かりやすくする |

---

## 4. 記録の付け方

`RESULTS.md` に週1回、実測値だけを追記する。予測・見込みは書かない。

---

## 5. 測定の限界（判断時に踏まえること）

- **広告ブロッカー利用者は解析ビーコンが遮断され、訪問者数に計上されない。**
  実測した確認用ブラウザでも遮断された。訪問者数は実際よりやや少なめに出る。
- `/go/` や `/search/none/` などのページ表示回数は遮断の影響を受けにくいため、
  比率の分子として比較的信頼できる。ただし分母（訪問数）が過小なら比率は過大に出る。
- 再訪者数はCookieなし解析のため精度に限界がある。参考値としてのみ扱う。
- `site:` 検索の結果件数は概算であり、正確な登録数はSearch Consoleでしか分からない。
