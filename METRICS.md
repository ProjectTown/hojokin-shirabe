# 需要検証プラン（公開後30日間・広告費ゼロ）

作成日: 2026-07-31

このサイトは**収益化を一切行わない状態**で公開し、需要があるかどうかだけを測る。
測定のために**トラッキングスクリプト・Cookie・個人情報**は一切使わない。

---

## 1. 測定の考え方

通常のアクセス解析は、訪問者にJavaScriptを実行させて行動を送信させる。
この方式は個人情報を扱うリスクがあり、今回の条件に合わない。

そこで**「測りたい行動が起きたら、必ず専用のURLが1回開かれる」**ようにサイトを設計した。
その結果、**ページごとの表示回数という最も基本的な数字だけ**で6つの指標が全て復元できる。

追加のスクリプトは不要。訪問者の識別も行わない。

---

## 2. 指標とURLの対応

| # | 測る指標 | 取得方法 | 実装済みの仕組み |
|---|---|---|---|
| 1 | 訪問者数（検索以外を含む） | 全ページの表示回数・参照元 | 通常のページ表示 |
| 2 | 再訪者数 | ホスティング側の集計 | ― |
| 3 | 補助金詳細の閲覧数 | `/s/*/` の表示回数 | 詳細ページ92件 |
| 4 | 公式サイトへの遷移数 | `/go/*/` の表示回数 | **中継ページ92件**。公式へのリンクは全てここを経由するので、表示回数＝遷移数になる |
| 5 | 検索結果ゼロ率 | `/search/none/` ÷ `/search/result/` | 0件のときだけ `/search/none/` へ遷移する |
| 6 | 実際に使われた検索条件 | `/find/{地域}/{業種}/` `/area/*/` `/industry/*/` の表示回数 | 絞り込み条件がそのままURLになる |

### 補足
- **検索キーワードは意図的に記録しない。** 検索語はURLのハッシュ（`#q=...`）に置いている。ハッシュはHTTPリクエストに含まれないため、サーバーにも集計にも残らない。「何回失敗したか」は分かるが「何を入力したか」は分からない、という設計。
- `/go/` は `robots.txt` と `noindex` で検索エンジンから除外している。
- 地域×業種は940通りすべてのページを生成済み。現時点では全組み合わせが1件以上ヒットするため、この軸での0件率は0%になる（`/search/none/` が0件の受け皿になる）。

---

## 3. 判定基準（30日後）

数値は「予測」ではなく**実測のみ**で判断する。

### 継続の条件（すべて満たす）

| 指標 | 最低ライン | 意味 |
|---|---|---|
| 訪問者数 | 30日で 100人以上 | 誰も来ないなら題材か導線が間違っている |
| 詳細ページ閲覧 | 訪問の 25%以上 | 一覧で帰るなら中身に価値がない |
| 公式サイトへの遷移 | 訪問の 8%以上 | **最重要**。ここが動けば「実際に使われた」と言える |
| 検索結果ゼロ率 | 40%未満 | 高いなら掲載範囲が狭すぎる |

### 判定

- **4つとも満たす** → 需要あり。次の段階へ進む
- **一部のみ** → 原因を特定し、**改善は1回だけ**行い、さらに30日測る
- **改善後も満たさない** → **事業を終了**。コードと測定結果を保存し、次の事業案へ移る

### 改善が1回だけ許される想定パターン

| 症状 | 想定原因 | 打ち手（1つだけ選ぶ） |
|---|---|---|
| 訪問者がほぼゼロ | そもそも見つけられていない | 掲載カテゴリを増やして入口を広げる |
| 一覧で離脱 | 情報が薄い / 見づらい | 詳細ページの構成を作り直す |
| 公式遷移が低い | 対象が自分に関係ないと判断されている | 対象条件の表示を分かりやすくする |
| ゼロ率が高い | 1カテゴリしか無いのが原因 | カテゴリを追加する |

---

## 4. 記録の付け方

`RESULTS.md` に週1回、実測値だけを追記する。予測・見込みは書かない。

```
## 第N週（YYYY-MM-DD 〜 YYYY-MM-DD）
- 訪問者数:
- 再訪者数:
- /s/ 表示回数:
- /go/ 表示回数:
- /search/result/ 表示回数:
- /search/none/ 表示回数:
- 表示回数の多かった条件ページ 上位5件:
```

---

## 5. 未解決事項（オーナーの判断が必要）

**上の指標のうち 1〜2（訪問者数・再訪者数）だけは、サイト側の作りでは取得できない。**
訪問者を数えるのはホスティング側の役割で、静的サイト単体では不可能なため。

現状の選択肢:

| 方法 | 費用 | 追加登録 | 6指標の取得可否 |
|---|---|---|---|
| A. GitHub Pages のみ | 無料 | GitHubのみ | **指標1・2が取れない**。3〜6も取れない（表示回数の集計機能がないため） |
| B. Cloudflare Pages（無料枠）+ 内蔵アクセス解析 | 無料 | Cloudflareの無料アカウント1つ | **6指標すべて取得可**。Cookie不使用・個人情報を集めない設計の解析 |

指標を1つも取れないと「需要を実測で判断する」という今回の目的自体が成立しないため、
**Bを推奨する。ただし無料アカウント登録が1つ必要になるため、オーナーの許可なしには進めない。**

いずれの場合も課金は発生しない。
